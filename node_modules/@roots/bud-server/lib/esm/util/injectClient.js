/**
 * Injects webpack entrypoints with HMR client scripts.
 *
 * Filters on `webpack.entry`
 */
export const injectClient = (app, injection) => {
  app.hooks.on('build/entry', entry =>
    Object.assign(
      {},
      Object.entries(entry).reduce(
        (entries, [name, asset]) =>
          Object.assign(Object.assign({}, entries), {
            [name]: Object.assign(Object.assign({}, asset), {
              import: [...asset.import, ...injection],
            }),
          }),
        {},
      ),
    ),
  )
}
//# sourceMappingURL=injectClient.js.map
