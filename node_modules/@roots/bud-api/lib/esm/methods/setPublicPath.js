import {isFunction, isString} from 'lodash'
const setPublicPath = function (publicPath) {
  isString(publicPath) &&
    this.hooks.on('location/publicPath', () => publicPath)
  isFunction(publicPath) &&
    this.hooks.on('location/publicPath', publicPath)
  return this
}
export {setPublicPath}
//# sourceMappingURL=setPublicPath.js.map
