import {isArray} from 'lodash'
const use = function (source) {
  if (!isArray(source)) {
    this.extensions.add(source)
    return this
  }
  source.forEach(extension => {
    this.extensions.add(extension)
  })
  return this
}
export {use}
//# sourceMappingURL=index.js.map
