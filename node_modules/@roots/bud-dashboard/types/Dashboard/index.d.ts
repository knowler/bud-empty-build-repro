import { Service as Base } from '@roots/bud-framework';
import { Instance } from 'ink';
export declare class Dashboard extends Base {
    name: string;
    dashboard: Instance;
    register(): void;
    run(): void;
    renderError(body: string, title: string): Instance;
    render(Component: any): Instance;
    kill(): void;
}
//# sourceMappingURL=index.d.ts.map