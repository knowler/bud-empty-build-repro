import {useEffect, useState} from 'react'
export const usePackageJson = ({discovery}) => {
  const [pkg, setPkg] = useState(null)
  useEffect(() => {
    if (!discovery) return
    setPkg(
      Object.assign(
        Object.assign(
          {},
          pkg !== null && pkg !== void 0 ? pkg : {},
        ),
        discovery.getProjectInfo(),
      ),
    )
  }, [discovery])
  return pkg
}
//# sourceMappingURL=usePackageJson.js.map
