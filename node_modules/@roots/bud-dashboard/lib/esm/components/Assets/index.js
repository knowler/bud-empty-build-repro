import React from 'react'
import {Box} from 'ink'
import {Asset} from './Asset'
/**
 * Asset component
 */
const Assets = ({stats, theme}) => {
  var _a
  return React.createElement(
    Box,
    {flexDirection: 'column', width: theme.bounds.width},
    (_a =
      stats === null || stats === void 0
        ? void 0
        : stats.assets) === null || _a === void 0
      ? void 0
      : _a.map((asset, id) =>
          React.createElement(
            Asset,
            Object.assign({theme: theme, key: id}, asset),
          ),
        ),
  )
}
export {Assets}
//# sourceMappingURL=index.js.map
