import React, {useState, useEffect} from 'react'
import {Logs} from './Logs'
/**
 * Console
 */
export const Console = ({progress}) => {
  const [logs, setLogs] = useState([])
  useEffect(() => {
    ;(progress === null || progress === void 0
      ? void 0
      : progress.message) && setLogs([...logs, progress.message])
  }, [
    progress === null || progress === void 0
      ? void 0
      : progress.message,
  ])
  return (logs === null || logs === void 0
    ? void 0
    : logs.length) > 0
    ? React.createElement(Logs, {logs: logs})
    : null
}
//# sourceMappingURL=index.js.map
