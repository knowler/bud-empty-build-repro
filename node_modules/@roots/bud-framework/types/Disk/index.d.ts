import { Service } from '../Service';
import { FileContainer } from '@roots/filesystem';
export declare abstract class Disk extends Service {
    name: string;
    private _baseDir;
    get baseDir(): string;
    set baseDir(baseDir: string);
    private _pattern;
    get pattern(): string[];
    set pattern(glob: string[]);
    abstract register(): void;
    abstract boot(): void;
    abstract make(key: string | number, options: {
        baseDir?: string;
        glob?: string[];
    }): FileContainer;
    protected abstract makeFileContainer(options: {
        baseDir?: string;
        glob?: string[];
    }): FileContainer;
}
//# sourceMappingURL=index.d.ts.map