import { Disk as Base } from '@roots/bud-framework';
import { FileContainer } from '@roots/filesystem';
export declare class Disk extends Base {
    register(): void;
    boot(): void;
    make(key: string | number, options?: {
        baseDir?: string;
        glob?: string[];
    }): FileContainer;
    protected makeFileContainer(options: {
        baseDir?: string;
        glob?: string[];
    }): FileContainer;
}
//# sourceMappingURL=index.d.ts.map