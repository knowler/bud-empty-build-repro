import { Service } from '@roots/bud-framework';
import { Dependencies as DependenciesManager } from '@roots/dependencies';
export declare abstract class Base extends Service {
    name: string;
    manager: DependenciesManager;
    abstract pkg(): string;
    abstract shouldInstall(dep: string, type: 'dependencies' | 'devDependencies'): boolean;
    abstract installDev(deps: {
        [key: string]: string;
    }, src: string): void;
    abstract install(deps: {
        [key: string]: string;
    }, src: string): void;
    abstract notify(notices: {
        msg: string;
        src: string;
    }[]): void;
}
//# sourceMappingURL=Base.d.ts.map