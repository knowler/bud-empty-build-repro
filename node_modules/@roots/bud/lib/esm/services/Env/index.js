import {__decorate} from 'tslib'
import {Service} from '@roots/bud-framework'
import {boundMethod as bind} from 'autobind-decorator'
import dotenv from 'dotenv'
import expand from 'dotenv-expand'
export class Env extends Service {
  constructor() {
    super(...arguments)
    this.name = 'service/env'
  }
  get envPath() {
    return this.app.path('project', '.env')
  }
  getParsedEnv() {
    return expand(dotenv.config({path: this.envPath})).parsed
  }
  bootstrap() {
    this.setStore(this.getParsedEnv())
  }
}
__decorate([bind], Env.prototype, 'getParsedEnv', null)
__decorate([bind], Env.prototype, 'bootstrap', null)
//# sourceMappingURL=index.js.map
