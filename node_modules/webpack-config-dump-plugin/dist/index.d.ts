interface PluginOptions {
    outputPath?: string;
    name?: string;
    depth?: number;
    keepCircularReferences?: boolean;
    showFunctionNames?: boolean;
    includeFalseValues?: boolean;
}
export declare class WebpackConfigDumpPlugin {
    private static getReferenceLabel;
    private static getFunctionLabel;
    private static getRef;
    readonly outputPath: string;
    readonly name: string;
    readonly depth: number;
    readonly keepCircularReferences: boolean;
    readonly showFunctionNames: boolean;
    readonly includeFalseValues: boolean;
    constructor(options?: PluginOptions);
    apply(compiler: any): void;
    getDump(config: any, depth: number | null, includeFalseValues: boolean, showFunctionNames: boolean): string;
    simplifyConfig(config: any, depth: number | null, includeFalseValues: boolean, showFunctionNames: boolean): any;
    dumpConfig(config: any): void;
    private simplifyLevel;
}
export {};
